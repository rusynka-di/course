<div class="container">
 <%# <% @questions.each do |question| %>
    <%# <p class="mb-sm"><%= link_to "Питання: #{question.id}", question_path(question.id)%>  <%#</p>
  <% end %> 
  <%# можна ще (id: question.id), (question.id) і (question)%>
  <%# <% @questions.each do |question| %>
  <%#<p><%= link_to "Питання: #{question.id}", "/questions/#{question.id}"%><%#</p> %>
  <%# <% end %>  <%# %>
  <p class="mt">
    <%= link_to "Створити питання", new_question_path, class: 'button button-main'%>
  </p>

  <p class="mt">
    <%= render 'form', question: @question %>
  </p>
</div>

<div class="container mt-sm">
  <h2 class="title-lg">
    <%= @questions.count %>
    <%= inclination(@questions.count, 'питання', 'питання', 'питань') %>
  </h2>
</div>

<% @questions.each do |question| %>
  <div class="mt-sm container">
    <%= render question %>
  </div>
<% end %>